Question-4
Write a script that accepts a filename as a command-line argument. The script should check if the file exists. If it does, it should print the number of lines and words in the file. If the file does not exist, it should print an appropriate message.